<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}User list from API.{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('build/app.css') }}">
        {% endblock %}
    </head>
    <body>
        {{ include('navbar.html.twig') }}
        <div class="container">
            <div class="starter-template">
                {% block body %}{% endblock %}
            </div>
        </div>
        {% block javascripts %}<script src="{{ asset('build/app.js') }}"></script>{% endblock %}
    </body>
</html>
